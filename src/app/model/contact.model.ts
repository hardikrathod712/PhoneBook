export class Contact {
    firstName: string;
    lastName: string;
    contact: string;

    constructor(fname: string,lname: string,con: string){
        this.firstName = fname;
        this.lastName = lname;
        this.contact = con;
    }
}